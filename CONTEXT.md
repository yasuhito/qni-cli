# qni-cli プロジェクト文脈

## 進め方の決定

- 最初の明確な成功条件は、AIエージェントが課題文から `qni` コマンド列を生成し、`qni-cli` で検証し、成否判定まで行う最小評価ループをエンドツーエンドで通すことにする。
- まず動く最小ループを維持し、その上に評価対象、検証能力、公開研究ログ、外部ベンチマーク連携を段階的に肉付けしていく。
- 技術的に重要なのは、常に「動く状態」を保ちながら、量子回路研究開発支援AIエージェント基盤を強力にしていくことである。
- 最小評価ループの入力は、既存 feature や元 Quantum Katas ではなく、このプロジェクト用に作る短いベンチマーク用課題ファイルとする。
- ベンチマーク用課題ファイルには、課題文、許可コマンド、期待結果、検証方法を明示し、自動評価・比較・拡張をしやすくする。
- ベンチマーク用課題ファイルの形式は Markdown + YAML frontmatter とする。AIエージェントには本文の自然文を読ませ、評価ランナーには frontmatter の構造化情報を読ませる。
- ベンチマーク用課題ファイルは `benchmarks/quantum-katas/` に置く。`features/katas/` は回帰テスト、`docs/` は説明文書、`benchmarks/` は評価対象データとして責務を分ける。
- 初期段階ではベンチマークをこのリポジトリに同梱する。将来ベンチマークが大規模化し、それ自体が成果物として独立できる段階になったら、別リポジトリへ分離することを検討する。
- 最初のベンチマーク集合は、`StateFlip`、`PlusState`、`BellState` のように、単一量子ビット・重ね合わせ・エンタングルメントを含む最小スモークセットとする。
- 1問だけではAIエージェント評価として弱く、全量では初期導入が重いため、量子回路設計の中核能力を最小限に横断する構成から始める。
- AIエージェントが最初に提出する成果物は `qni` コマンド列だけとする。`circuit.json` を直接出させず、説明文も初期採点対象には混ぜない。
- まずは「課題文 → `qni` コマンド列 → 実行 → 検証」の純粋な評価ループを確立する。将来は既存ベンチマークを参考にしながら、ベンチマーク形式やAIエージェント出力形式を改善する。
- 最小評価ループの採点は、生成された `qni` コマンド列を実行した後、`qni run` や `qni expect` の出力を期待値と比較して自動合否判定する方式とする。
- 初期段階では、実行成功だけではなく量子回路として期待結果に到達したかを確認する。ただしゲート数、深さ、レビューエージェントによる批評は後続段階で追加する。
- 評価ランナーは、最初からAIエージェントを内部で呼び出さず、AIや人間が生成した `qni` コマンド列ファイルを受け取って採点する方式にする。
- AI呼び出しと評価実行を分離し、人間、AIエージェント、将来の複数モデルを同じ提出物形式で比較できるようにする。
- 提出物は、1行1コマンドの `.qni` テキストファイルとする。素の shell script は任意コマンド実行になるため避ける。
- 評価ランナーは `.qni` ファイルを読み、許可された `qni` コマンドだけを実行する。
- `.qni` ファイルの各行は、`qni add H --qubit 0 --step 0` のように実際のCLIと同じ完全なコマンドを書く。
- AIエージェントに実際に使うCLIを書かせ、人間もそのままコピーして再現できる形式にする。
- 評価ランナーは、課題ごとに一時ディレクトリを作り、その中で `circuit.json` を生成・検証する。
- `qni-cli` は現在ディレクトリの `circuit.json` を読むため、評価実行が開発中の作業ディレクトリを汚染しないようにする。
- 最小評価ループの実装言語は TypeScript / Node.js とする。
- 評価基盤は、Markdown + YAML frontmatter、JSON結果出力、GitHub Pages連携、外部ベンチマーク連携へ広げる可能性があるため、TypeScript / Node.js で実装する。
- 最小評価ランナーのコマンド名は `qni benchmark run` とする。
- `benchmark` をCLIの正式な概念として扱い、将来 `qni benchmark list`、`qni benchmark report`、`qni benchmark init` などへ自然に拡張できるようにする。
- `qni benchmark run` は、最初は課題ファイル1つと提出 `.qni` ファイル1つを明示的に受け取る。
- まず「1課題・1提出物」の評価を安定させ、複数課題一括実行は後続段階で追加する。
- ベンチマーク課題ファイルの frontmatter は、最初は `id`, `title`, `source`, `difficulty`, `allowed_commands`, `checks` を持つ。
- `reference_solution` は評価時に隠すべきなので同じ課題ファイルには入れない。`setup` や `tags` は必要になった段階で追加する。
- `checks` の最初の種類は、`run` の状態ベクトル比較と `expect` の期待値比較の両方をサポートする。
- `StateFlip` や `PlusState` は状態ベクトル確認が自然で、`BellState` は `ZZ` や `XX` の期待値確認も自然であるため、最小スモークセットでは両方を扱う。
- `checks` の数値比較は課題ファイルごとに `tolerance` を指定できるようにする。初期値は `1e-9` を想定するが、角度計算や将来のシミュレーション差分に備えて固定値をランナーへ埋め込まない。
- `allowed_commands` は課題ファイルごとに許可サブコマンドを列挙する。
- 課題ごとに許可操作を明示し、安全性と表現力を両立する。`StateFlip` のような課題では `qni add` だけで十分だが、将来の角度変数を使う課題では `qni variable` も許可できるようにする。
- 最小評価ループでは、AIエージェントに検証コマンドは書かせない。提出物は回路生成コマンド列だけに限定し、検証は課題ファイルの `checks` で固定する。
- 採点の信頼性はベンチマーク側が持つ。AI自身の検証方針や説明は、将来の研究ログや上位評価で扱う。
- `qni benchmark run` の出力は、通常は人間向けテキストを標準出力し、`--json` オプションで機械処理向けJSONも出せるようにする。
- Markdownレポート生成は、GitHub Pages段階で後から追加する。
- 最初のエンドツーエンド成功条件は、最小スモークセット3問、各提出 `.qni`、`qni benchmark run` による全合格、さらに `--json` 出力まで含める。
- 1問だけでは評価として弱く、GitHub Pagesまで含めると初期範囲が広すぎる。JSON出力まであれば、次段階で日記、集計、複数モデル比較へ自然に進められる。
- 最初のエンドツーエンド実装に入る前に、`features/cli/benchmark_run.feature.md` を追加して `qni benchmark run` の振る舞いを Cucumber で定義する。
- `qni benchmark run` は新しいCLI機能なので、課題ファイルと `.qni` 提出物を受け取り、一時ディレクトリで実行し、合否とJSONを出す振る舞いを feature で先に固定してから実装する。
- 最初の `features/cli/benchmark_run.feature.md` には、合格ケース、失敗ケース、`--json` 出力ケース、許可されていないコマンドの拒否ケースを含める。
- 最小評価ランナーの中核は、正解を通すだけでなく、不正解を落とし、機械処理できる出力を出し、AIが生成した不許可な提出物を安全に拒否することである。
- 最初のスモークセット3問は、`StateFlip`, `PlusState`, `BellState` とする。
- `StateFlip` で単一量子ビットの基本ゲート、`PlusState` で重ね合わせ、`BellState` で2量子ビットのエンタングルメントを最小に横断する。
- スモークセット3問は、`benchmarks/quantum-katas/basic-gates/state-flip.md`, `benchmarks/quantum-katas/superposition/plus-state.md`, `benchmarks/quantum-katas/superposition/bell-state.md` に置く。
- ID は `basic-gates/state-flip`, `superposition/plus-state`, `superposition/bell-state` のように、ディレクトリ構造と対応する形式にする。
- このグリルの次の成果物は、今決めた内容をもとにしたPRDとする。
- 評価基盤、ベンチマーク形式、提出形式、将来拡張を含む小さなプロダクトとして先にPRDへ落とし、その後に feature、実装、issue分割へ進む。
- PRD は `docs/prd/quantum-circuit-agent-benchmark.md` に置く。
- `docs/superpowers/specs/` は設計検討、`features/` は振る舞い仕様、`docs/prd/` は要求定義として責務を分ける。
- PRD の範囲は、最小評価ループを中心にしつつ、非目標と将来拡張も明示する。
- 初回実装スコープは膨らませないが、`.qni` 形式、`--json` 出力、外部ベンチマーク接続などの設計意図は将来拡張として残す。
- PRD のMVP受け入れ条件は、3問の合格、失敗提出の不合格判定、不許可コマンド拒否、`--json` 出力まで確認できることとする。
- MVPの中核は、正解を通す、不正解を落とす、危険な提出物を拒否する、機械処理できる結果を出す、の4点である。
- PRD作成後の issue 分割は、「課題ファイル作成」「feature追加」「ランナー実装」「JSON出力」「安全性」のように技術要素ごとに分ける。
- 特に「安全な `.qni` 実行」「frontmatter の読み取り」「検証ロジック」は失敗モードが違うため、同じ issue に詰め込まず、レビューしやすい単位に分ける。
- issue の実装順序は、feature → 課題ファイル3つ → frontmatter parser → `.qni` 安全実行 → checks → CLI出力/JSON とする。
- 新機能前に feature を置くリポジトリ規則に従い、先に `qni benchmark run` の振る舞いを固定してから、評価データ、読み取り、実行、検証、出力の順に積む。
- `/to-issues` 方針に合わせて vertical slice issue として GitHub issue #251〜#258 を作成済み。実装は #251 から始める。
- MVPでは、AIに `.qni` を生成させるための最小プロンプトテンプレートを `benchmarks/prompts/` に置く。
- 評価ランナーはAIを直接呼ばないが、課題ファイルから `.qni` 提出物を作る標準手順を残し、人間、Pi、Codex、Claudeなどの違いを超えて同じ条件で提出物を作れるようにする。
- MVPでは、標準解 `.qni` を `benchmarks/solutions/` に含めるが、課題ファイルとは分離する。
- 標準解は回帰テストやサンプル提出に必要だが、AIに見せる入力と答えが同居しないようにする。
- 標準解ディレクトリは、`benchmarks/solutions/quantum-katas/basic-gates/state-flip.qni` のように課題と対応する構造にする。
- 課題ファイルとは分離しつつ、パス構造は対応させ、由来と対応関係を明確にする。
- MVPでは、不正解サンプル `.qni` も1つ置く。
- MVP受け入れ条件に「不正解を落とす」があるため、`StateFlip` に対して `H` を置く提出など、明確に失敗するサンプルを1つ用意する。
- 不正解サンプルは `benchmarks/incorrect/quantum-katas/...` に置く。
- 正解の標準解と不正解サンプルは明確に分け、将来「失敗パターン集」として拡張できるようにする。
- MVPでは、不許可コマンドのサンプル `.qni` も1つ置く。
- 不正解は「許可された操作だが量子回路として誤り」、不許可は「安全性・仕様違反」として分ける。
- 不許可コマンドのサンプルは `benchmarks/disallowed/quantum-katas/...` に置く。
- 不正解と不許可は失敗理由が違うため、後で評価結果を `wrong answer` と `invalid submission` に分けて集計できるようにする。
- `qni benchmark run` の終了コードは、合格、不合格、不許可、実行エラーを区別する。
- 具体的には、合格 `0`、不合格 `1`、不許可 `2`、実行エラー `3` とする。
- CIや自動集計で使える評価コマンドにするため、終了コードでも失敗種別を判別できるようにする。
