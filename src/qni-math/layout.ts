export interface CellDimensions {
  widthPx: number;
  heightPx: number;
}

export interface RasterLayout {
  widthPx: number;
  heightPx: number;
  columns: number;
  rows: number;
  scale: number;
}
