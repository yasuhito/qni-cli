# 既存の研究試行ディレクトリは上書きしない

`qni research record` の初期機能では、保存先の `research/runs/<timestamp>-<slug>/` が既に存在する場合、既存ディレクトリを上書きせずに失敗する。研究試行ログは証跡であり、同じ名前の再実行で過去の試行を暗黙に壊してはいけない。

この場合、コマンドは研究試行ディレクトリを変更せず、終了コード `3` で失敗する。エラーメッセージでは、別の `--slug` を指定するよう案内する。必要になった場合の `--force` は後続機能として検討する。
